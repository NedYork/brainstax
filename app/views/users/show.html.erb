<h2><%= current_user.username %></h2>

<h3>Subjects</h3>
<ul>
  <% current_user.subjects.each do |subject| %>
    <li>
      <%= link_to(subject.title, subject_url(subject)) %>
      <!-- <%= button_to "Remove subject", subject_url(subject), method: :delete %> -->
    </li>
  <% end %>
</ul>

<%= link_to "Add Subject", new_subject_url %>
